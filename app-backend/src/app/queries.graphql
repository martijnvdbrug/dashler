type Query {
  Dashboard(id: ID!): Dashboard!
}

type Mutation {
  createDashboard(input: DashboardInput): Dashboard
  addBlock(dashboardId: ID!, input: BlockInput): Dashboard
  removeBlock(dashboardId: ID!, blockId: ID!): Dashboard
}

input DashboardInput {
  name: String
}

input BlockInput {
  name: String
  buttons: [ButtonInput]
}

input ButtonInput {
  label: String
  url: String
}
